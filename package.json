{
  "name": "portfolio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i src/input.css -o dist/styles.css --minify && cp index.html dist/ && cp src/app.js dist/",
    "dev": "tailwindcss -i src/input.css -o dist/styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
